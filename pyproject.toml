[project]
name = "key_value_store"
version = "0.1.0"
dependencies = []

[project.scripts]
key_value_store = "key_value_store.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]